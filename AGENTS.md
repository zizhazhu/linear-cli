# AGENTS.md

本文件为在此代码库中工作的 AI Agent 提供指导。

## 项目是什么

Linear CLI：自用的 Linear 命令行工具。官方未提供 CLI，第三方接口也可能随时
变化，故自制一个薄 CLI 用于本地同步状态。

## 导航协议

- 每个目录有且仅有一个 `ABOUTME.md` 作为该目录的自描述入口，人类和 Agent 都读它。
- 进入任何目录工作前，先读该目录的 `ABOUTME.md`。
- 想了解某个子目录，去读那个子目录的 `ABOUTME.md`，不要从父级文件推断子目录细节。
- 「内容」清单不列 `node_modules`、`.git` 等惯例文件与产物，只列有信息量的条目。

## 顶层地图

| 条目 | 说明 |
|------|------|
| `README.md` | 项目门面（人类向）：项目定位与使用说明 |
| `docs/` | 技术文档（设计决策、技术方案等） |
| `src/` | 源码（`linear_cli` 包，uv 项目 + src layout） |
| `tests/` | 单元测试（pytest） |

新增顶层条目（目录或关键文件）时，同步更新本表；新增目录时同时创建其
`ABOUTME.md`。

## Git 与提交

提交信息用英文，遵循 Conventional Commits：`<type>: <description>`。

- subject 行 ≤ 50 字符（至多 72），用祈使句，结尾不加句号；概括提交做了什么。
- body 解释为什么这样改：动机、约束、取舍等 diff 里看不出来的信息；
  diff 本身能读出的内容不重复写。

## Commit 纪律

- 测试代码与生产代码不得出现在同一 commit。
- 只有测试 commit 允许引入新的失败测试（红灯）。
- 每个生产代码 commit 必须使至少一个此前失败的测试通过、且此前通过的测试不回归；
  或者是不改变任何测试结果的重构/杂务 commit，须在 commit message 中声明。
- 最终实现 commit 前，全量测试必须通过。

## 测试

- 每条测试意图以 Given/When/Then 原文写进对应测试函数的 docstring，测试代码即测试文档。
- 真实 API 测试采用合约点清单制：每个合约点恰好对应一条真实 API 测试，
  且该测试只断言合约（字段格式、读回一致性、错误结构）；
  其余测试一律离线，并须证明未调用 API（可用 spy）。

## 功能开发流程

每个新功能按「探查 → 测试 → 实现」三步走：

1. **探查**：调用官方 Linear MCP（`mcp__linear__*` 工具）实测目标操作，确认
   字段全集、参数语义与错误结构。MCP 是 GraphQL API 的衍生视图，仅作规格
   参照；字段与行为的真值以 GraphQL API 为准。
2. **写测试**：将探查确认的合约固化为测试，仍遵循合约点清单制——每个合约点
   恰好一条真实 API 测试，其余离线并证明未触网。
3. **实现**：按测试手写 GraphQL query/mutation，落地 CLI 命令。

MCP 不进入测试回路：其 OAuth 凭证无法复用于 pytest，在线依赖也违反离线
纪律。回归网是固化后的合约测试，MCP 只在开发时在场。
